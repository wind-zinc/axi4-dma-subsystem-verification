+tree tb_uvm_top.dut

/* SVA checker modules are verification logic, not DUT implementation RTL.
 * Their hierarchy rows contain only line/toggle code metrics plus assertion
 * coverage.  Exclude line/toggle while retaining assertion coverage. */
begin line+tgl -module axi_dma_bus_sva end
begin line+tgl -module axi_dma_internal_sva end
begin line+tgl -module axi_dma_axil_sva end
