/* A leading '+' makes assertion coverage default to excluded.  Enable the
 * DUT-bound checkers plus the master-facing AXI-Lite checker; this continues
 * to exclude implementation assertions inside uvm_pkg. */
+tree tb_uvm_top.dut
+tree tb_uvm_top.axi_dma_axil_sva_inst
